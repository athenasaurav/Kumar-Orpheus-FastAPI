# Local environment variables for Docker Compose
# Copy this file to .env.local and Docker Compose will automatically use it

# Set these to your user's UID and GID for proper container permissions
UID=$(id -u)
GID=$(id -g)
